<%= form_with(model: operation) do |form| %>
  <% if operation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(operation.errors.count, "error") %> prohibited this operation from being saved:</h2>

      <ul>
        <% operation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :entry, style: "display: block" %>
    <%= form.check_box :entry %>
  </div>

  <div>
    <%= form.label :payment_method, style: "display: block" %>
    <%= form.select :payment_method, options_for_select([["Cartão", "Cartão"], ["Pix", "Pix"], ["Dinheiro", "Dinheiro"]]) %>
  </div>

  <div>
    <%= form.label :value, style: "display: block" %>
    <%= form.text_field :value %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
